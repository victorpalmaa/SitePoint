{
  "version": 2,
  "installCommand": "yarn --cwd frontend install",
  "buildCommand": "yarn --cwd frontend build && rm -rf build && mkdir -p build && cp -R frontend/build/* build/",
  "outputDirectory": "build",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/" }
  ]
}