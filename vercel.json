{
  "version": 2,
  "installCommand": "yarn --cwd frontend install",
  "buildCommand": "yarn --cwd frontend build && rm -rf build && cp -R frontend/build ./build",
  "outputDirectory": "build",
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
