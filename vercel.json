{
  "version": 2,
  "buildCommand": "yarn --cwd frontend install && yarn --cwd frontend build",
  "outputDirectory": "frontend/build",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/" }
  ]
}